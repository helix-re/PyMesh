import PyMesh
import numpy as np


def lattice_sort_topology(lattice, contour):
    return PyMesh.lattice_sort_topology(lattice,contour);

def lattice_detect_segments(lattice):
    return PyMesh.lattice_detect_segments(lattice);
